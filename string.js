let First_name="sumit"
let Last_name="kasoudhan"
//console.log(`My First_name is ${First_name}`);
//console.log(`"My name is:" ${First_name} ${Last_name}`);
  function Fullname(first, last){
    return `${first}${last}`
  }
  let Name=`Hello ${Fullname(First_name,Last_name)}`
  console.log(Name)